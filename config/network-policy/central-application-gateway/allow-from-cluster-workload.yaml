apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kyma-project.io--acm-gateway-allow-from-cluster-workload
  namespace: system
spec:
  podSelector:
    matchLabels:
      app: central-application-gateway
  policyTypes:
    - Ingress
  ingress:
    - ports:
      - protocol: TCP
        port: 8080
      - protocol: TCP
        port: 8082
