apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kyma-project.io--acm-gateway-allow-from-health-check
  namespace: system
spec:
  podSelector:
    matchLabels:
      app: central-application-gateway
  policyTypes:
    - Ingress
  ingress:
    - ports:
      - protocol: TCP
        port: 8081